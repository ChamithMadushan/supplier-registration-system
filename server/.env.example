PORT=4000
NODE_ENV=development

# Base URL used in emails / links (frontend)
FRONTEND_URL=http://localhost:5173

# JWT - change this in production
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# SQLite
DB_PATH=./data/supplier.db

# Uploads
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=10
